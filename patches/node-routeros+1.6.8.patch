diff --git a/node_modules/node-routeros/dist/Channel.js b/node_modules/node-routeros/dist/Channel.js
index ef56575..40a686f 100644
--- a/node_modules/node-routeros/dist/Channel.js
+++ b/node_modules/node-routeros/dist/Channel.js
@@ -119,20 +119,22 @@ class Channel extends events_1.EventEmitter {
         if (packet.length > 0 && !this.streaming)
             this.emit('data', parsed);
         switch (reply) {
-            case '!re':
-                if (this.streaming)
-                    this.emit('stream', parsed);
-                break;
-            case '!done':
-                if (!this.trapped)
-                    this.emit('done', this.data);
-                this.close();
-                break;
-            default:
-                this.emit('unknown', reply);
-                this.close();
-                break;
-        }
+    case '!re':
+        if (this.streaming)
+            this.emit('stream', parsed);
+        break;
+    case '!empty':
+        break;
+    case '!done':
+        if (!this.trapped)
+            this.emit('done', this.data);
+        this.close();
+        break;
+    default:
+        this.emit('unknown', reply);
+        this.close();
+        break;
+}
     }
     /**
      * Parse the packet line, separating the key from the data.
diff --git a/node_modules/node-routeros/tsconfig.json b/node_modules/node-routeros/tsconfig.json
index 3ef7a24..8a0444d 100755
--- a/node_modules/node-routeros/tsconfig.json
+++ b/node_modules/node-routeros/tsconfig.json
@@ -2,6 +2,7 @@
     "compilerOptions": {
         "target": "es6",
         "module": "commonjs",
+        "ignoreDeprecations": "6.0",
         "moduleResolution": "node",
         "outDir": "dist",
         "inlineSourceMap": true,
